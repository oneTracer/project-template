import * as types from './mutation-types'
export default {
  // setReportInfo ({ commit }, { courseA, courseB }) {
  //   const reportInfo = { courseA, courseB }
  //   commit(types.SET_REPORT_INFO, reportInfo)
  // },
}
